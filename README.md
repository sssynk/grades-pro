# grades-pro

a simple electron app to let people in the issaquah school district look up their grades. 

unfinished, all keys in code are non-functional. a .env file is required for use.

prerequisites:
python
npm
node

setup:
clone git or download,
cd into directory,
python setup.py,
npm start

created by james
only tested on mac
